<!-- Subs.Ext\Tools\Rating.WPF\Templates\DataTemplates.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:prism="http://prismlibrary.com/"
                    xmlns:enum="clr-namespace:Rating.WPF.Enums"
                    xmlns:v="clr-namespace:Rating.WPF.Views">

    <DataTemplate x:Key="SubtitleTemplate">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                       VerticalAlignment="Stretch"
                       Text="{Binding Position}">
            </TextBlock>

            <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4"
                       Margin="5,0,0,0"
                       Text="{Binding Text}">
            </TextBlock>

            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                       Margin="5,0,0,0"
                       Text="Rating: ">
            </TextBlock>

            <TextBlock Grid.Row="1" Grid.Column="2"
                       Margin="5,0,0,0"
                       Text="{Binding RatingCurrent}">
            </TextBlock>

            <Button Grid.Row="1" Grid.Column="4"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Right"
                    Content="Promote"
                    Command="{Binding DataContext.PromoteSubtitleCommand, RelativeSource={RelativeSource AncestorType={x:Type v:WorkspaceView}}}"
                    CommandParameter="{Binding}">
            </Button>

            <Button Grid.Row="1" Grid.Column="5"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Right"
                    Content="Demote"
                    Command="{Binding DataContext.DemoteSubtitleCommand, RelativeSource={RelativeSource AncestorType={x:Type v:WorkspaceView}}}"
                    CommandParameter="{Binding}">
            </Button>

        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="PrimaryFileTemplate">
        <DataTemplate.Resources>
            <ObjectDataProvider x:Key="dataFromEnum"
                            MethodName="GetValues"
                            ObjectType="{x:Type System:Enum}">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="enum:LanguageEnum" />
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
        </DataTemplate.Resources>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                       Margin="5"
                       TextTrimming="CharacterEllipsis"
                       Text="{Binding FileName}"
                       ToolTip="{Binding FilePath}">
            </TextBlock>
            <ComboBox Grid.Row="1" Margin="5"
                      VerticalAlignment="Top"
                      ItemsSource="{Binding Source={StaticResource dataFromEnum}}"
                      SelectedItem="{Binding Language, Mode=TwoWay}">
            </ComboBox>
            <ListView Grid.Row="2" Margin="5" Grid.ColumnSpan="2"
                      HorizontalContentAlignment="Stretch"
                      AlternationCount="2"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      ItemsSource="{Binding SubtitleCollection}"
                      ItemTemplate="{StaticResource SubtitleTemplate}"
                      SelectedItem="{Binding DataContext.PrimaryFileSubtitleSelectedItem, RelativeSource={RelativeSource AncestorType={x:Type v:WorkspaceView}}, Mode=TwoWay}">
            </ListView>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="SecondaryFileTemplate">
        <DataTemplate.Resources>
            <ObjectDataProvider x:Key="dataFromEnum"
                            MethodName="GetValues"
                            ObjectType="{x:Type System:Enum}">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="enum:LanguageEnum" />
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
        </DataTemplate.Resources>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Row="1" Margin="5"
                      VerticalAlignment="Top"
                      ItemsSource="{Binding Source={StaticResource dataFromEnum}}"
                      SelectedItem="{Binding Language, Mode=TwoWay}">
            </ComboBox>
            <ContentControl Grid.Column="1" Content="{Binding SubtitleSelectedItem}" ContentTemplate="{StaticResource SubtitleTemplate}"/>
        </Grid>
    </DataTemplate>

</ResourceDictionary>